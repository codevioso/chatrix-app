import React from 'react';
import {View} from "react-native";

function TeamConversationScreen({navigation}) {
    return (
        <View>Conversation</View>
    );
}

export default TeamConversationScreen;
